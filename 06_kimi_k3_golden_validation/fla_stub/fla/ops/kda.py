"""Stub for fla.ops.kda -- see package __init__.py for why this exists."""


def chunk_kda(*args, **kwargs):
  raise NotImplementedError(
      "fla stub: chunk_kda was actually called -- this means something "
      "outside the intended MoE-only golden-bundle path was exercised."
  )


def fused_recurrent_kda(*args, **kwargs):
  raise NotImplementedError(
      "fla stub: fused_recurrent_kda was actually called -- this means "
      "something outside the intended MoE-only golden-bundle path was exercised."
  )
